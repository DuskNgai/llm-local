name: agent-local
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - anthropic
      - "huggingface_hub[cli]"
      - mlx-lm
      - openai
      - gnureadline
      - python-dotenv
      - pyyaml
